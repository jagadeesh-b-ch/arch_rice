return {
  {
    "nvim-neo-tree/neo-tree.nvim",
    opts = {
      filesystem = {
        -- Combines empty nested folders into a single line (e.g., src/main/java)
        group_empty_dirs = true,
      },
    },
  },
}
